import { useEffect } from "react";
import THREE from "three";

export function helloThree() {
  useEffect(() => {
    const Scene = new THREE.Scene()

    const object = new THREE.Mesh()
    
    const d = new THREE.Camera()

  }, [])
}